"""LSEG quantitative analytics platform.

Top-level package. The library lives under `lseg_quant.sdk` (thin SDK wrappers),
`lseg_quant.analytics` (numerics), `lseg_quant.universe` (identifier resolution),
and `lseg_quant.io` (output writing). Entry-point scripts live in `workflows/`
at the repo root and are NOT importable from this package.
"""

__version__ = "0.1.0"
